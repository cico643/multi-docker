sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t cico643/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true cico643/react-test npm test

after_success:
  - docker build -t cico643/multi-client ./client
  - docker build -t cico643/multi-nginx ./nginx
  - docker build -t cico643/multi-server ./server
  - docker build -t cico643/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push cico643/multi-client
  - docker push cico643/multi-nginx
  - docker push cico643/multi-server
  - docker push cico643/multi-worker
